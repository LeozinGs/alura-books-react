import './styles.css';
import Logo from '../Logo/Logo';

const Header = () => {
    return (
        <header className='app-header'>
            <Logo />
        </header>
    );
}

export default Header;